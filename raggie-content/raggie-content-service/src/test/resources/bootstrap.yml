spring:
  application:
    name: content-service
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      config:
        namespace: ${spring.profiles.active}
        group: raggie-project
        file-extension: yaml
        refresh-enabled: true
        shared-configs:
          - data-id: logging-${spring.profiles.active}.yaml
            group: raggie-project
            refresh: true
  profiles:
    active: dev
#spring:
#  application:
#    name: content-service
#  datasource:
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://127.0.0.1:3306/raggie_content?serverTimezone=UTC&userUnicode=true&useSSL=false
#    username: root
#    password: 123
#logging:
#  config: classpath:log4j2-dev.xml